# merge_sort.cpp
Merge Sort is a divide-and-conquer algorithm. It works by dividing the list into two halves repeatedly until each sublist contains only one element. Since a single element is already sorted, the algorithm then merges the sublists back together in sorted order. 
